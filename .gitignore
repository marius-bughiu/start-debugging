# Astro site (in /site)
site/node_modules/
site/dist/
.astro/
site/.astro/
site/.cache/

# Root vite cache (not committed)
/node_modules/

# Generated OG images and derived logo (produced by scripts/generate-og-images.mjs)
site/public/og/
site/public/logo-512.png

# Distribution credentials (see site/.env.example). Never commit these.
site/.env
site/.env.local
.env
.env.local

# Editorial / content-ops working set (kept private)
/content-strategy/
/TRAFFIC_ROADMAP.md

# Local-only MCP server config
/.mcp.json

# OS/editor
.DS_Store
Thumbs.db
.claude/settings.local.json
/.claude/worktrees
